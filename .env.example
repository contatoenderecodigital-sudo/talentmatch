# Copie pra .env e preencha com os valores do projeto Supabase (Settings > API).
# NUNCA commite o .env. Só a anon key entra aqui — service_role JAMAIS (SECURITY.md §2).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
